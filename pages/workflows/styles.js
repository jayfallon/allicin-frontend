import styled from 'styled-components';

const WorkflowsStyles = styled.section`
  /* background-color: #fff; */
  padding: 5rem 3rem;
  box-shadow: ${props => props.theme.bs};
`;

export default WorkflowsStyles;
